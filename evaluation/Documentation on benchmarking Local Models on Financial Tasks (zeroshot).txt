Based on the zero shot approach using the FLARE-FIQASA dataset and Meta’s Llama-3.2-1B model, the partial matching evaluation can be used for grading model predictions in tasks like sentiment classification. This will perform better on models that produce extra text or rephrase labels as it will check if the correct sentiment appears anywhere in the model's response, instead of just searching for an exact match or answer. Additionally, concurrency and token streaming can speed up the evaluation pipeline, allowing for more efficient processing across different transformer tasks. By logging the model’s outputs and gold labels in a CSV file, you can identify which examples differ from the expected results. Comparing exact-match and partial-match results directly will help us better understand the model’s current performance and see where it might need adjustments or improved prompts.